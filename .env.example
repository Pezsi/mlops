# MLOps Pipeline Environment Configuration
# Copy this file to .env and fill in your actual values

# ============ Email Notification Settings ============
# Gmail SMTP settings (you need to generate an App Password)
# Instructions: https://support.google.com/accounts/answer/185833
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-character-app-password
TO_EMAILS=admin@example.com,team@example.com

# ============ Slack Notification Settings ============
# Create a Slack Incoming Webhook
# Instructions: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ============ Webhook API Settings ============
# API key for securing webhook trigger endpoints
WEBHOOK_API_KEY=mlops-secret-key-2025-change-this

# ============ MLOps Metadata Database ============
# These are set in docker-compose.yml, but can be overridden here
# METADATA_DB_CONN=postgresql://mlops_user:mlops_password@mlops-metadata-db:5432/mlops_metadata

# ============ Airflow Settings ============
# AIRFLOW_UID=50000
# AIRFLOW_GID=0

# ============ MLflow Settings ============
# MLFLOW_TRACKING_URI=file:/app/mlruns

# ============ Flask Debug Mode ============
# Set to "true" only for development
FLASK_DEBUG=false

# ============ Notes ============
# After creating .env file, restart the services:
#   docker-compose down
#   docker-compose up -d
